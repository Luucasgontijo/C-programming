{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Run C Program",
            "type": "shell",
            "command": "mkdir -p bin && gcc \"${file}\" -o \"bin/${fileBasenameNoExtension}\" && \"bin/${fileBasenameNoExtension}\"",            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Compile and run C program, placing binaries in the 'bin' folder"
        }
    ]
}

